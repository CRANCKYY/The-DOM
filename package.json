{
  "name": "dom-flatbook",
  "version": "1.0.0",
  "description": "DOM Flatbook Lab",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 5000 --reporter mocha-junit-reporter test/*.js"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}